specVersion: "v1beta"
serviceId: "capstonebeta"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "capstonebeta-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
